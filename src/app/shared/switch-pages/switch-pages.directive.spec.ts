import { SwitchPagesDirective } from './switch-pages.directive';

describe('SwitchPagesDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchPagesDirective();
    expect(directive).toBeTruthy();
  });
});
